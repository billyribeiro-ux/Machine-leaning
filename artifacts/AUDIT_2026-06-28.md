# Four-Agent Adversarial Audit — 2026-06-28

Four parallel specialist auditors (ML-leakage, validation-math, execution/risk,
quant-architecture) audited the repo. Every finding was verified against exact
code; several were reproduced numerically. This file records what was FIXED in
this pass and what remains OPEN.

## Fixed in this pass (with regression tests where applicable)

| # | Sev | Defect | Fix |
|---|-----|--------|-----|
| 1 | P0 | `bfill()` in FeatureEngineer injected FUTURE values into rolling-window warm-up rows (genuine look-ahead; inflated fold-0 / IPO-name metrics) | ffill-only + callers drop 260 warm-up rows/symbol (`src/ml/data_pipeline.py`, both experiment scripts) |
| 2 | P0 | Deflated Sharpe fed ANNUALIZED Sharpe into a per-period formula (t-stat inflated ~sqrt(252); a Sharpe-0.06 strategy scored DSR 0.97) | `deflated_sharpe_ratio(periods_per_year=...)` deflates internally; finite-guard added (`src/backtest/validation.py`) |
| 3 | P0 | Kurtosis convention off-by-3 (formula wanted raw, callers passed pandas excess) | Formula now takes EXCESS kurtosis; BacktestValidator's +3 removed |
| 4 | P0 | market-neutral PBO call returned a deterministic ~1.0 constant; K-fold "PBO" ranked folds, not strategies | Both fabricated PBO numbers removed (report NaN + reason); PBO dropped from the edge gate |
| 5 | P0 | Sortino used std of negative subset (emitted values like -4.5e14) | RMS-of-min(r,0) convention; hand-computed regression test |
| 6 | P0 | `PaperBroker.equity()` silently dropped positions with missing prices | Raises KeyError; rebalance refuses to trade unpriced held symbols (tested) |
| 7 | P0 | No leverage guard (5x gross filled silently, cash -$400k) | `max_gross_leverage=1.0`; rebalance rejects violations; sells-before-buys (tested) |
| 8 | P0 | Dealer-GEX sign INVERTED vs the cited Simple Model in scanify + scanify_0dte engines (long/short gamma verdicts flipped) | Signs flipped to standard (+call/-put dealer gamma) in both engines |
| 9 | P0 | scanify_0dte backtester read nonexistent `chain.contracts` -> zero fills forever | `contracts`->`quotes`; enum-safe option_type compare |
| 10 | P0 | RL TradingEnvironment minted cash on shorts / burned notional on longs; equity = cash + unrealized-PnL dropped principal | Signed-proceeds close accounting; `equity = cash + position*price`; flat-price roundtrip invariant verified (loses only costs) |
| 11 | P1 | RL short-cover PnL sign inverted | Negated |
| 12 | P1 | PPO GAE tensor-shape crash (PPO had never actually run) | `.view(-1)` / `.view(1)` |
| 13 | P1 | `n_trials=1` dishonest across sequential experiments | n_trials=10 (conservative effective-trials) in both scripts |

## Open (documented, NOT yet fixed) — ranked

- P1: paper_trade fills at the same close its signal used (optimistic by up to ~5-8%/yr); fix = fill at next-day open. Also: paper window overlaps the final model's training data — hold out last ~290 days before saving the artifact and stamp `train_end_date`.
- P1: PurgedKFoldCV ignores `label_spans` (index-cut purge, embargo < 30-day feature window at fold edges; adequate today only by parameter luck). Implement span-based purging.
- P1: TFT 3-output "quantile" head has no quantile loss; OnlineLearner trains col 1 while inference reads col 0 — align or shrink head to 1 output.
- P1: scanify charm sign ∂Δ/∂(T) vs ∂Δ/∂t inverted; charm-flow threshold units (per-year vs per-day); expiry hardcoded 20:00 UTC (wrong in winter); mixed IV day-count across strikes.
- P1: per-symbol turnover netting in train_and_validate's daily_portfolio understates costs (bounded ~1bp/day).
- P1: risk/management.py Sortino NaN guard + wrong convention; Calmar mixes unrelated series; Kelly computed but never applied; VaR sign/seed issues.
- P2: three parallel GEX engines disagree — consolidate to one (delete/delegate options_0dte/spx_engine.py); mock-data fallbacks should raise, not silently substitute; alpha/alternative_data.py "FinancialBERT" is random-weights noise — quarantined status should be explicit; EnsembleModel drops uncertainties / decorative confidence weights; replay-buffer self-reinsertion; BacktestValidator's rigged PBO inputs.

## Pre-live checklist (none exist yet; ALL required before real Schwab orders)
Kill switch; buying-power gate; per-name position cap; max daily loss halt;
duplicate-order guard (client order IDs); order-rate limiter; price sanity
checks; whole-share rounding; position reconciliation vs broker; market-hours
gate; short locate/borrow-cost model; append-only order audit log.

## Verdict impact
Every fixed statistical bug biased TOWARD declaring an edge, yet both prior
experiments still said "no edge" — those negative verdicts stand and are now
stronger. The framework previously could NOT be trusted to certify a positive
result; after fixes 1-5 + 13 the gates actually gate. Experiments re-run with
corrected math; see updated artifacts/*.json.
